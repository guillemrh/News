version: '3'
services:
  my_app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: news_api
    image: news_api
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    restart: always

